# Tools
- [`inference/`](inference/) — Megatron dynamic-inference HTTP server and client
  - It can do chat loop or just a simple one message step.
- [`merge/`](merge/) — Megatron backend for distributed checkpoint merging.
